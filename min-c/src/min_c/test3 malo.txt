main{
 int := num<-50 ;

 bool := var <-false ;

 string := vari <- "algo";
 int := num <- 49;
 

 >.esto funciona para algo?

    if(num>=num2)then{
         int := num2;
    }else
	>. holi
	}

	while(true){
		>_
			existo?
		_<
}

}

func int := FuncionRetornable (int := num, string := cringe) { 
    
	>. algo aqui

	return variable
}